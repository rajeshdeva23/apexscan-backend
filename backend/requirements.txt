# ---------------------------------------------------------------------------
# ApexScan backend — pinned runtime requirements.
# Kept in sync with pyproject.toml for environments that do not use PEP 621.
# Install dev tooling with: pip install -e ".[dev]"
# ---------------------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
alembic>=1.14
pydantic>=2.10
pydantic-settings>=2.7
redis>=5.2
python-json-logger>=3.2
