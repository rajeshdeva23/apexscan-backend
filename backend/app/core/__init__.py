"""Core package.

Cross-cutting concerns shared by the whole application: configuration,
logging, and security primitives. This layer has no dependency on the API,
services, or persistence layers — everything else may depend on it.
"""
