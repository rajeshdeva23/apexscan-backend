"""API package.

HTTP boundary of the application. Depends on services and dependencies, never
the other way around. Versioned sub-packages (``v1`` …) keep the public
contract stable as the platform evolves.
"""
