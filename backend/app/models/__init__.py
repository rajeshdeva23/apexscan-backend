"""ORM models package (placeholder).

SQLAlchemy models inheriting from :class:`app.database.Base` live here — one
module per aggregate (instruments, strategies, scan results, …). Alembic
imports this package to discover metadata for migrations. No tables are
defined in Phase 1.
"""
