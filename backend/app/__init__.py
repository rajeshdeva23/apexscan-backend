"""ApexScan backend application package.

Top-level package for the FastAPI service. Sub-packages follow a clean
architecture layout (api → services → repositories → models) so that the
core domain never depends on frameworks or external providers.
"""
