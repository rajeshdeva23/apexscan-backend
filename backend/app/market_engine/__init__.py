"""Market engine package (placeholder).

The market engine will orchestrate live/historical market data flowing from
broker adapters into strategies: subscription management, candle aggregation,
and the scan loop. It depends on the adapter interface, not concrete brokers.

No engine logic is implemented in Phase 1 — structure only.
"""
